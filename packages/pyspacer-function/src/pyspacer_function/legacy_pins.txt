# Runtime pins for the legacy (Beta) serving image: both the image's install
# manifest and the runtime assertion. check_legacy_pins() parses this file and
# refuses to serve when an installed version differs, which is the legacy
# format's stand-in for the graph format's model.json "trained_with" gate.
# One file, so the install and the check cannot drift.
#
# Captured from the production MERMAID API image (ECR asset tag
# f5410d7241bfbd85b7369ac7ad564b3e416fd0512b51a1a4d7578d0275c24f50 in
# cdk-hnb659fds-container-assets-554812291621-us-east-1) via pip freeze on
# 2026-09-11. The Lambda image that actually runs is arm64/Python 3.12, while
# this capture is from x86_64/Python 3.13; these pins freeze versions, not
# wheels, and the pre-release gate measures the equivalence.
pyspacer==0.12.0
scikit-learn==1.5.2
scipy==1.18.0
torch==2.6.0
torchvision==0.21.0
numpy==2.1.3
Pillow==12.3.0
